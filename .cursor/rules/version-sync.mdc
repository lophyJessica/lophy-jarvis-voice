---
description: 每轮源码改动后必须递增 versions/v{n} 并同步 AGENTS.md 当前版本号
alwaysApply: true
---

# 版本号同步（强制收尾）

在本仓库**任何一轮**修改 `src/`、`public/`、`index.html`、样式或构建相关文件后，**同一轮对话内**必须完成版本收尾，不得只改代码不记版本。

## 1. 确定新版本号 n

- 查看 `versions/` 下已有 `v*` 目录，**n = 当前最大数字 + 1**（如已有 v17 → 本轮为 v18）。
- 仅文档/规则、未动可部署源码时：可不递增 n，在汇报中说明「无源码变更」。

## 2. 物理快照

- 创建 `versions/v{n}/`，按源码路径复制**本轮改动的文件**（保留相对路径，如 `versions/v{n}/src/App.tsx`）。
- 新增 `versions/v{n}/README.md`：一行标题 + 改动文件清单 + 简要说明（3～5 行即可）。
- **禁止**删除旧 `versions/v*` 目录。

## 3. 同步 AGENTS.md（易漏，必做）

- 更新 `## 当前状态` 小节：
  - 日期 + **当前版本号 v{n}**
  - 版本表：至少保留最近 4～5 个版本的「版本 | 摘要」一行说明；新版本写在表首。
- 若能力清单有变，同步勾选/条目，避免仍写旧行为（例如「仅语音模式 TTS」等过时描述）。

## 4. 汇报格式（对用户）

每轮结束必须包含：

- **当前版本号 v{n}**
- **改动文件清单**
- **build**：`npm run build` 结果（同轮）
- **jarvis-voice.zip**：同轮 `npm run build` 后必须更新；汇报中写明 **`ls -la jarvis-voice.zip` 的修改时间（mtime）**、大小、主 bundle 文件名（见 AGENTS.md §2.1）

## 5. 与红线一致

- 版本记录 ≠ git commit；无用户明确要求不 commit/push。
- 不修改 `lophy-jarvis`（文字版）目录。
