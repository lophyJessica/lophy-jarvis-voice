---
description: 每轮任务完成后自动上传 zip 到 VPS + 提交自检报告（产物自动流转管道）
alwaysApply: true
---

# 产物自动流转管道（完成后必做）

与 `task-closeout.mdc` 配套。**每轮 build 完成后**，除了常规收尾（版本快照/AGENTS.md 更新），必须执行以下两个动作：

## 1. 上传 zip 到 VPS（自动部署）

```bash
rsync -avz -e "ssh -p 2222 -i ~/.ssh/id_ed25519_vps" "/Users/liulongfei/个人文件/lophy-jarvis-voice/jarvis-voice.zip" root@192.220.14.245:/var/www/pmlophy.com/jarvis-voice-incoming/
```

VPS 每分钟 cron 检测 incoming/ 新 zip → 自动部署到 jarvis-voice/ → 热更新生效。上传成功后回复里注明「已上传 VPS」。

## 2. 提交自检报告（审核管道）

生成自检报告，命名 `{项目}-{任务}-{时间}.md`，模板：

```markdown
# AI 自检报告
- 项目/任务：
- 改动文件清单：
- 每个改动点说明：
- 自检结果（build/测试是否通过）：
- 遗留风险/待确认：
```

上传（复制整条执行）：

```bash
curl -X POST "https://pmlophy.com/p/jarvis/file/upload" \
  -H "X-Jarvis-User: ai-reports" \
  -F "file=@{报告文件名}"
```

完成后在回复里附上报告文件名（杰西卡会读目录审核）。

## 注意

- zip 在项目**根目录**（不是 app/），路径含中文要加引号
- 红线不变：不 commit / 不 push；部署由管道自动完成，不需要用户手动发 zip
