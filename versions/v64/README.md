# v64

- 图片预览右上角关闭控件改为主题绿背景 `#10A37F`。
- 控件图标改为白色，悬停/聚焦使用深绿色 `#0B7A61`。
- 未修改图片处理、文件上传、滚动条、登录、语音、TTS 及历史同步逻辑。
- 本轮文件：`src/App.css`、`AGENTS.md`。
- `npm run build` 通过，主包为 `index-D89u1pQX.js`。
- 本地浏览器实测按钮背景色为 `rgb(16, 163, 127)`，图标颜色为白色。
- 自查截图：`v64-image-control.jpg`；无 Console error。
