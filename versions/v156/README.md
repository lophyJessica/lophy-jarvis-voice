# v156 — 罗宾头像换成菲儿插画

## 改动文件
- `app/src/version.ts`（`APP_VERSION=v156` + `ROBIN_AVATAR_URL`）
- `app/src/App.tsx`（顶栏 brand-avatar 用菲儿图）
- `app/src/components/LoginPage.tsx`（登录页统一为 brand-avatar 插画）
- `app/src/App.css`（avatar img 圆形 cover）

## 说明
将文字「R」换成菲儿（灰泰迪）插画 `robin-avatar-sm.png`；主界面与登录页统一。保留原圆形尺寸。
注：v155 已占用，本轮递增为 v156。

## 自检截图
- `v156-feier-avatar.png`：顶栏圆形菲儿头像 + v156
