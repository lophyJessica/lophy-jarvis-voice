# v33 — 切模式不停播报 + 语音钮入输入框

## 改动文件
- `src/App.tsx`
- `src/App.css`

## 说明
- 进入/退出语音模式不再 `cancelSpeech` / `stopCurrentTurn`；纯切模式时 TTS 继续，发送/停止/开麦说话等仍会终止。
- 主操作钮（语音波形 / 关闭 / 发送 / 停止）移入输入框右侧，波形钮样式对齐参考图（黑圆 + 三道白竖条）。
