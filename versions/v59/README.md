# v59

- 修复 v58 的宽度规则同时作用于附件预览和输入框，导致二者横向并排的问题。
- 底部 composer 改为纵向堆叠：附件预览在上、输入框在下，最大宽度均为 `792px`。
- 补齐文档附件 chip 的布局、间距、圆角及长文件名省略样式。
- 未修改附件处理、上传、消息、语音、登录、历史同步及 API 逻辑。
- 本轮文件：`src/App.css`、`AGENTS.md`。
- `npm run build` 通过，主包为 `index-B51dSx6Z.js`。
- 本地浏览器注入真实图片附件后实测：预览区与输入框均为 `792px`，左边界同为 `564px`，预览位于输入框上方。
- 自查截图：`v59-attachment-stacked.jpg`；无 Console error。
