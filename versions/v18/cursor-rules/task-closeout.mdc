---
description: 每轮任务结束必须汇报版本号、文件清单与 zip 状态（语音版仓库）
alwaysApply: true
---

# 任务收尾清单（项目内）

与 `version-sync.mdc` 配套。每轮改完**同一对话内**收尾：

1. `versions/v{n}/` + `README.md`
2. 更新 `AGENTS.md` → `## 当前状态`
3. 回复用户时必须出现：**v{n}**、**改动文件**、**build**、**jarvis-voice.zip**（适用时）

默认：用户未禁止时，功能改动后应 `npm run build` 并更新 `jarvis-voice.zip`。
