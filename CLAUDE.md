# 项目规则入口

所有项目规则、工作流规范、目录结构、命名约定等，统一维护在 `AGENTS.md`。

**Claude Code 操作须知：**
- 读取项目规则 → 读 `AGENTS.md`
- 新增或更新规则 → 写入 `AGENTS.md`，不要写到本文件
- 本文件（`CLAUDE.md`）仅作入口索引，保持最简，不存实质内容

> 项目通用规则统一维护在 `AGENTS.md`，其他工具或入口文件只做引用，避免同一条规则在多个地方写出不同版本。
